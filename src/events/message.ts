import Message from '../extensions/Message';
import Logger from '../util/Logger';
import { Client } from 'discord.js';

export default function(this: Client, msg: Message) {
	// TODO: implement handler? export message to handler?
}
